server:
  port: ${MESSAGING_SERVICE_PORT:8104}

spring:
  application:
    name: messaging-service
  datasource:
    url: ${MESSAGING_SERVICE_DB_URL:jdbc:postgresql://localhost:5432/viaverse_messaging}
    username: ${MESSAGING_SERVICE_DB_USERNAME:viaverse}
    password: ${MESSAGING_SERVICE_DB_PASSWORD:viaverse}
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when_authorized
  metrics:
    tags:
      application: ${spring.application.name}

logging:
  structured:
    format:
      console: ecs
  level:
    app.viaverse: INFO

---
spring:
  config:
    activate:
      on-profile: local
logging:
  level:
    app.viaverse: DEBUG

---
server:
  port: 0
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: ${MESSAGING_SERVICE_TEST_DB_URL:jdbc:postgresql://localhost:5432/viaverse_messaging_test}
