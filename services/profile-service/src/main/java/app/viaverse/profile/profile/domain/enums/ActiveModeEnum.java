package app.viaverse.profile.profile.domain.enums;

/**
 * UI-facing mode the client should present first for an account.
 */
public enum ActiveModeEnum {
    CUSTOMER,
    INDIVIDUAL_PROVIDER,
    BUSINESS
}
