package app.viaverse.profile.profile.domain.enums;

/**
 * Visibility tier applied when rendering a public profile preview.
 */
public enum PublicVisibilityEnum {
    PUBLIC,
    LIMITED,
    PRIVATE
}
